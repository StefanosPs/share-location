import React from "react"; 
import { Container } from "react-bootstrap";

const Loading = () => {
 
	return (
		<Container fluid>
		<div>Loading...</div>
		</Container>
	);
};

export default Loading;
